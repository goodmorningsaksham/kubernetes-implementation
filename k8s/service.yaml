apiVersion: v1
kind: Service
metadata:
  name: kubernetes-implementation-api-service
  labels:
    app: kubernetes-implementation-api
spec:
  selector:
    app: kubernetes-implementation-api
  ports:
    - protocol: TCP
      port: 3000 # Service port
      targetPort: 3000 # Container port
  type: NodePort # change to LoadBalancer if running in cloud