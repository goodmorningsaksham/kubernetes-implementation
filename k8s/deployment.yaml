apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubernetes-implementation-api
  labels:
    app: kubernetes-implementation-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: kubernetes-implementation-api
  template:
    metadata:
      labels:
        app: kubernetes-implementation-api
    spec:
      containers:
        - name: kubernetes-implementation-api
          image: sakshamg17/kubernetes-implementation-api:latest
          ports:
            - containerPort: 3000
          env:
            - name: NODE_ENV
              value: "production"
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            httpGet:
              path: /ready
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 20